<resources>
    <style name="AppTheme.Light" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Other item definitions -->
        <item name="android:textColor">@color/light_text_color</item>
        <item name="android:windowBackground">@color/light_background_color</item>
        <item name="colorSurface">@color/light_surface_color</item>
    </style>

    <style name="AppTheme.Dark" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Other item definitions -->
        <item name="android:textColor">@color/dark_text_color</item>
        <item name="android:windowBackground">@color/dark_background_color</item>
        <item name="colorSurface">@color/dark_surface_color</item>
    </style>

    <style name="AppTheme.Default" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Other item definitions -->
        <item name="android:textColor">@color/default_text_color</item>
        <item name="android:windowBackground">@color/blueInput</item>
        <item name="colorSurface">@color/default_surface_color</item>
    </style>
</resources>